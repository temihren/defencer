export const TIME_SCALE = 0.7;

export const GRAVITY = 0.8;

export const BULLET_SIZE = 10;

export const BULLET_SPEED = 50
    * TIME_SCALE;

export const ENEMIES_SPAWN_RATE = 2 // Per second
    * TIME_SCALE;

export const FIRE_RATE = 20 // Per second
    * TIME_SCALE;
